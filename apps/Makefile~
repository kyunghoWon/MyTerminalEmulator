main: main.o
	gcc `pkg-config --cflags gtk+-3.0` main.o -o main -L../lib -lfuncs `pkg-config --libs gtk+-3.0`

main.o: main.c
	gcc `pkg-config --cflags gtk+-3.0` -g -c -I../include main.c -o main.o `pkg-config --libs gtk+-3.0`

clean:
	rm -f main.o main
